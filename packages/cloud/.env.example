# Functions config is managed via `firebase functions:config:set`.
# This file documents the expected keys for local reference.

# jwt.secret: random hex string used to sign project tokens
# signing.private: ed25519 private key (base64) used to sign audit/consent records
# (Stripe removed) No payment configs needed
